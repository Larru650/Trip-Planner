var vow = require('./lib/vow');

var p = new vow.Promise((resolve, reject) => {
    setTimeout(() => {
        reject('!!!!');
    }, 10);
});

p.then().then();

process.on('unhandledRejection', function() {
    console.log(arguments);
});